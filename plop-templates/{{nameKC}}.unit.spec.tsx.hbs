import React from "react";
import {render, fireEvent, screen,} from '@testing-library/react'
import '@testing-library/jest-dom'


import {{nameTC}} from ".";

describe("{{nameTC}} Area", () => {
    // eg basic unit test for render
    it("should render the {{nameTC}} something", () => {
        const component = render(<{{nameTC}} MacGuffin="test instance props" />);
        expect(component.getByTestId("{{nameKC}}__someSubDiv")).toBeInTheDocument();
      });
})